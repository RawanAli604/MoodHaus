<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
      <%- include('../partials/_navbar.ejs') %>

<h1>Edit Moodboard</h1>

<% if (!board) { %>
  <p>Board not found.</p>
<% } else { %>
  <form action="/moodboards/<%= board._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
    <div>
      <label>Title<br>
        <input type="text" name="title" value="<%= board.title %>" required maxlength="120">
      </label>
    </div>

    <div>
      <label>Description<br>
        <textarea name="description" rows="4"><%= board.description || '' %></textarea>
      </label>
    </div>

    <div style="margin-top:8px;">
      <button type="submit">Save changes</button>
      <a href="/moodboards/<%= board._id %>" style="margin-left:8px;">Back</a>
    </div>
  </form>
<% } %>

</body>
</html>