<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Edit Furniture</title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Cinzel+Decorative:wght@400;700&family=Montserrat:wght@300;400;600&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <div class="edit-wrapper">

  <div class="edit-head">
    <h1>Edit <%= furniture.name %></h1>
  </div>

  <form action="/furnitures/<%= furniture._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="edit-form">

    <div class="input-box">
      <label for="name">Name</label>
      <input type="text" name="name" id="name" value="<%= furniture.name %>" required>
    </div>

    <div class="input-box">
      <label for="description">Description</label>
      <textarea name="description" id="description" rows="4" required><%= furniture.description %></textarea>
    </div>

    <div class="input-box">
      <label for="category">Category</label>
      <input type="text" name="category" id="category" value="<%= furniture.category %>" required>
    </div>

    <div class="input-box">
      <label for="price">Price</label>
      <input type="number" name="price" id="price" min="0" value="<%= furniture.price %>" required>
    </div>

    <% if (furniture.image) { %>
      <div class="current-image">
        <p>Current Image:</p>
        <div class="img-wrapper hover-zoom">
          <img src="<%= furniture.image %>" alt="<%= furniture.name %>">
          <div class="overlay">Current</div>
        </div>
      </div>
    <% } %>

    <div class="input-box file-input-box">
      <label for="image" class="file-label">Change Image</label>
      <input type="file" name="image" id="image" accept="image/*">
    </div>

    <button type="submit" class="btn-primary submit-btn">Update Furniture</button>
    
  </form>
</div>
</body>
</html>
